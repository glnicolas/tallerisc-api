const path = require('path');

module.exports = {
  'config': path.resolve('src/config', 'database.js'),
  'models-path': path.resolve('src', 'models'),
  'env': process.env.NODE_ENV || 'development'// for  Sequelize-CLI use only
};